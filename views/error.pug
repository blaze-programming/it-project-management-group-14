extends layout

block content
  h1.text-danger= message || 'Error'
  if error && error.status
    p.lead Status: #{error.status}
  if error && error.stack && error.stack.length
    pre.bg-light.p-3.rounded= error.stack
  a.btn.btn-secondary(href='/') Return to Dashboard
